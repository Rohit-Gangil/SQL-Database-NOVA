# --- Secrets ---
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.yaml

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
venv/
.venv/
env/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.hypothesis/

# --- Data (generated, never committed) ---
data/raw/
data/interim/
data/processed/
data/external/
*.parquet
*.duckdb
*.duckdb.wal
*.csv
!tests/fixtures/*.csv
!**/seeds/*.csv

# --- Models & experiment tracking ---
models/
# Large model artifacts stay out; the small CSV/JSON that back every reported
# number are committed so a reviewer can check them without a 25-minute rerun
# (audit M-5), and so the static site can be built from a clone.
artifacts/*
!artifacts/*.csv
!artifacts/*.json
!artifacts/*.txt
mlruns/
mlartifacts/
*.onnx
*.pkl
*.joblib
*.ckpt
lightning_logs/

# --- dbt ---
target/
dbt_packages/
logs/
.user.yml

# --- Dagster ---
.dagster/
dagster_home/
storage/
history/

# --- Docker / DB volumes ---
pgdata/
*.dump

# --- OS / editor ---
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~

# --- Oracle SQL Developer leftovers ---
*.log
*.lst
